/** Phone Activity panes — same ≤767 breakpoint as cashier (`POS_CASHIER_PHONE_MAX_WIDTH`). */
export type PosActivityPhonePane = "list" | "detail";

export const POS_ACTIVITY_PHONE_PANE_DEFAULT: PosActivityPhonePane = "list";
