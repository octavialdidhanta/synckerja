/** Local QR display / cancel TTL (seconds). Xendit QR may live longer until expire RPC. */
export const POS_QRIS_TTL_SECONDS = 60;

/** Pending checkout DB TTL — must be >= POS_QRIS_TTL_SECONDS. */
export const POS_PENDING_TTL_SECONDS = 90;
