import { XenditSettingsPanel } from "@/4-0-xendit-settings/components/XenditSettingsPanel";

/** Legacy standalone page export — use `/xendit/connect` module tab. */
export default function XenditSettingsPage() {
  return <XenditSettingsPanel />;
}
