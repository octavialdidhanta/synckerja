/** Desktop/web build: camera capture not wired; modal is a no-op shell if opened. */
export function CameraModal(_props: { open: boolean; onClose: () => void; onCapture: (file: File) => void }) {
  return null;
}
