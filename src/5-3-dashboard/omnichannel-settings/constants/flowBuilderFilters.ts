import type { FlowBuilderStatusFilter } from "@/5-3-dashboard/omnichannel-settings/types/flowBuilder.types";

/** Meta WhatsApp Flow active-flow quota shown in the listing header. */
export const FLOW_BUILDER_ACTIVE_FLOWS_LIMIT = 100;

export const FLOW_BUILDER_STATUS_FILTERS: FlowBuilderStatusFilter[] = ["all", "active", "draft"];

export const FLOW_BUILDER_LISTING_PAGE_SIZE = 10;
