# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

# Bun (project uses npm + package-lock.json; lockfiles optional local artifacts)
bun.lock
bun.lockb

# Local reference clone (no longer a git submodule; optional for development)
synckerja-reference/

dist
dist-ssr
*.local

# TypeScript incremental cache (e.g. after `tsc --build`)
*.tsbuildinfo

# Env (keep secrets out of git)
.env
.env.*
!.env.example

# Cursor agent scratch (not source)
.cursor/_payroll*
.cursor/debug*.log

# Supabase: temporary chunk/invoke artifacts (real migrations use YYYYMMDDHHMMSS_*.sql)
supabase/migrations/_*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
